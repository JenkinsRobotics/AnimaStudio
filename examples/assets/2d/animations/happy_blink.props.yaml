schema: asset-props/v1

# First hand-authored sidecar — demonstrates the format end-to-end.
# Operator extends + replaces the other 166 as they get curated.

name: "Happy Blink"
author: "Mochi project"
license: "MIT"

mood: happy
tags: [eyes, blink, happy, expressive, default]
hint: "Quick happy eye blink — use for short positive reactions."

# Rendering hints
ideal_size: [64, 64]
framing: fit
playback_speed: 1.0
loop: true

# Type-specific properties for the eye-animation JSON renderer.
type_props:
  blink_interval_ms: 3500
  pupil_color: [255, 255, 255]
  bg_color: [0, 0, 0]
