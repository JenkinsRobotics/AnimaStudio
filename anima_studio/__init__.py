"""anima_studio — open-source unified character animation system for AI
robots: digital avatars and physical animatronics from one rig, one
format, one authoring tool.

Will export the ``module.yaml`` factory entrypoint once the JaegerOS
module is real (see the ``*.example`` files in this package). For now
the package is the home of the ``.anima`` format runtime.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
